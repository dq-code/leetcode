class Solution(object):

    def wordBreak(self, s, wordDict):
        """
        :type s: str
        :type wordDict: Set[str]
        :rtype: bool
        """
        if s=='': return True


        map = [False for x in range(len(wordDict))]
        for i in range(len(s)):
            if

        return False



        